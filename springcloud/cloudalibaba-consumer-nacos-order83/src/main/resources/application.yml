server:
  port: 83

spring:
  application:
    name: nacos-order-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #要去哪里获得供应者列表
#根据服务名动态获取服务提供者
server-url:
  nacos-order-service: http://nacos-payment-provider

